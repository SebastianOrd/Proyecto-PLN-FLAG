# Data Sources

The `data-sources` folder contains datasets from Cochrane. These datasets are organized into folders and subfolders, each containing text data used for training and testing machine learning models.